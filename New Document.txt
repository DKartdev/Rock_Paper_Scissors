Pseidocode

create function getRndInteger() where using math.random u will get function that gives random number from 1 to 3.

Function getComputerChoice.It  randomly returns rock paper or scissors.
with if statements state number and wich word will show, rock paper or scissors.

Test with console.log if works as intended.


function getHumanChoice.

make a variable what vill get value from promt. so human enters in promt value for variable tht represents humanCoice.
input needs to be accepted in all cases, cant be case sensitive. but need to accept only rock, paper, scissors

Players score.

humanScore and computerScore in global scope.

Logic for rounds.

if paper && rock === paper, if paper && scissors === scissors;  

if rock && paper === paper, if rock && scissors === rock;

if scissors && paper === scissors, if scisssors && rock === rock.

increment score by result.

if player > computer vaariable ++10;
if player < computer variable ++10.

levels/rounds 


5 repetitions of game. for loop possibly. in 5th round in the end say who wins.



if case 1 (rock, paper)

console.log(Paper wins!)

scoore count

k'a izdar'it t'a lai vi'n's saprot ka katru reizi kad ir jauns rauds punkti j'askaita kl'at nevis no 0 


every raund need to be counted together not starting from 0 every time 
